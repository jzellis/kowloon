var figures = require('figures');
var cliCursor = require('cli-cursor');
var chalk = require('chalk');
var dayjs = require('dayjs');
var formattedLog = function formattedLog(options, output) {
  var timestamp = dayjs().format(options.dateFormat);

  // eslint-disable-next-line no-console
  console.log("".concat(chalk.dim("[".concat(timestamp, "]")), " ").concat(output));
};
var renderHelper = function renderHelper(task, event, options) {
  var log = formattedLog.bind(undefined, options);
  if (event.type === 'STATE') {
    var message = task.isPending() ? 'started' : task.state;
    log("".concat(task.title, " [").concat(message, "]"));
    if (task.isSkipped() && task.output) {
      log("".concat(figures.arrowRight, " ").concat(task.output));
    }
  } else if (event.type === 'TITLE') {
    log("".concat(task.title, " [title changed]"));
  }
};
var _render = function render(tasks, options) {
  var _iterator = _createForOfIteratorHelper(tasks),
    _step;
  try {
    var _loop = function _loop() {
      var task = _step.value;
      task.subscribe(function (event) {
        if (event.type === 'SUBTASKS') {
          _render(task.subtasks, options);
          return;
        }
        renderHelper(task, event, options);
      }, function (err) {
        // eslint-disable-next-line no-console
        console.log(err);
      });
    };
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      _loop();
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
};
var VerboseRenderer = /*#__PURE__*/function () {
  function VerboseRenderer(tasks, options) {
    _classCallCheck(this, VerboseRenderer);
    this._tasks = tasks;
    this._options = Object.assign({
      dateFormat: 'HH:mm:ss'
    }, options);
  }
  _createClass(VerboseRenderer, [{
    key: "render",
    value: function render() {
      cliCursor.hide();
      _render(this._tasks, this._options);
    }
  }, {
    key: "end",
    value: function end() {
      cliCursor.show();
    }
  }], [{
    key: "nonTTY",
    get: function get() {
      return true;
    }
  }]);
  return VerboseRenderer;
}();
module.exports = VerboseRenderer;